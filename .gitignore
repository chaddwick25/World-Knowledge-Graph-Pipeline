# Python
__pycache__/
__pycache__


# TODO: look into removing this, only use the docker image
# Poetry
.venv/

# IDE / Editor
.vscode/

# Caches
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Env

Logs
*.log
# Backend

.env
compose.override.yml

# ── Runtime data artifacts (generated by init_planet / pipeline, not tracked) ──
# All live in BASE_DATA_DIR (/app/data in Docker, mounted via ./backend/data:/app/data).
# The directory itself stays as a bind-mount target; its contents are gitignored.
backend/data/country_relations.json
backend/data/geofabrik_index.json
backend/data/mapqa_parser/
backend/data/graph_artifacts/
backend/data/spectral_reports/
backend/data/pbf_cache/
backend/data/models/
backend/data/projection_training/
backend/data/logs/
# Root symlink (backward compat)
/mapqa_template_mapping.csv

docs


# Backup files
*.bk

# docs
backend/data/geodata
backend/backend/data

*__pycache__ 